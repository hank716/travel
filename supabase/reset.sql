-- =============================================================================
-- reset.sql — 清空資料重來（在 Supabase Dashboard → SQL Editor 執行）
-- ⚠️ 不可復原！執行前請確定真的要清。
-- 不會刪除資料表結構，只清資料；跑完不需重跑 schema.sql。
-- =============================================================================

-- ---------------------------------------------------------------------------
-- 選項 A（預設）：只清「行程相關資料」，保留登入帳號
--   會清掉：所有行程、成員(membership)、幣別、行程項目、記帳、分帳、匯率快取
--   會保留：帳號(auth.users) 與 profiles（你仍是管理員，免重新註冊）
-- ---------------------------------------------------------------------------
truncate table public.trips cascade;   -- cascade 連帶清 trip_currencies / members / itinerary_items / expenses / expense_splits
truncate table public.fx_cache;

-- ---------------------------------------------------------------------------
-- 選項 B（可選）：連「所有帳號」一起清光，徹底重來
--   想用就把下面這行的註解拿掉執行。
--   刪 auth.users 會 cascade 清掉 profiles；之後第一個重新註冊的人＝新管理員。
--   （members 已在選項 A 被清空，故不受影響）
-- ---------------------------------------------------------------------------
-- delete from auth.users;
